= AI Assistant
:revdate: 2026-03-18
:page-revdate: {revdate}
:description: SUSE Observability Self-hosted

AI Assistant is disabled by default in self-hosted SUSE Observability. When you enable it, the Helm chart turns on the AI Assistant feature in the UI and deploys two backend components:

* the AI Assistant UI, which is already deployed but hidden behind the feature flag
* the AI Assistant backend service
* the MCP (Model Context Protocol) server used by the AI Assistant backend

The feature is controlled by the Helm value `ai.assistant.enabled`.

== Enable AI Assistant

Set the feature flag in your Helm values:

[,yaml]
----
ai:
  assistant:
    enabled: true
----

After enabling the flag, the chart exposes the AI Assistant UI and deploys the `suse-observability-ai-assistant` StatefulSet, the `suse-observability-mcp` Deployment, and the related Services and ServiceAccounts.

[NOTE]
====
The Helm chart configures the AI Assistant backend to use AWS Bedrock by default by setting `USE_BEDROCK=true`.
====

== Configure MCP and AI Assistant

Most AI Assistant settings should stay at their chart defaults. In particular, it is recommended not to change internal connectivity settings such as listen addresses, service ports, or the built-in environment variables unless you have explicit guidance for your deployment.

In practice, the most important settings are:

* `stackstate.components.mcp.replicaCount` to scale the MCP server
* `stackstate.components.mcp.resources` to size the MCP server
* `stackstate.components.aiAssistant.resources` to size the AI Assistant backend
* `stackstate.components.aiAssistant.serviceAccount.annotations` to attach an AWS IAM role for Bedrock access

=== MCP configuration

The MCP server is enabled automatically together with AI Assistant. The main setting to tune is `stackstate.components.mcp.replicaCount`. Increase it when you need more MCP capacity.

You can also set `stackstate.components.mcp.resources` to match the expected workload.

=== AI Assistant backend configuration

For the AI Assistant backend, the most important settings are `stackstate.components.aiAssistant.resources` and `stackstate.components.aiAssistant.serviceAccount.annotations`.

Use `stackstate.components.aiAssistant.resources` to control CPU and memory allocation.

Use `stackstate.components.aiAssistant.serviceAccount.annotations` to attach the IAM role required for AWS Bedrock.

The persistence settings (`stackstate.components.aiAssistant.persistence.size` and `stackstate.components.aiAssistant.persistence.storageClass) can also be adjusted if you want a different PVC size or storage class for the SQLite database.

Example:

[,yaml]
----
ai:
  assistant:
    enabled: true

stackstate:
  components:
    mcp:
      replicaCount: 2
      resources:
        requests:
          cpu: "250m"
          memory: "512Mi"
        limits:
          cpu: "1"
          memory: "512Mi"
    aiAssistant:
      resources:
        requests:
          cpu: "500m"
          memory: "1Gi"
        limits:
          cpu: "2"
          memory: "1Gi"
      serviceAccount:
        annotations:
          eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/suse-observability-ai-assistant
      persistence:
        size: 5Gi
----

This example focuses on the settings most often changed in production: MCP scaling, CPU and memory sizing, the IAM role annotation, and optional AI Assistant persistence sizing.

== Configure the ServiceAccount for AWS Bedrock

Only the AI Assistant backend needs AWS credentials for Bedrock. The MCP server does not call Bedrock.

On Amazon EKS, the recommended approach is IAM Roles for Service Accounts (IRSA). Add the IAM role annotation to the AI Assistant ServiceAccount:

[,yaml]
----
ai:
  assistant:
    enabled: true

stackstate:
  components:
    aiAssistant:
      serviceAccount:
        annotations:
          eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/suse-observability-ai-assistant
      extraEnv:
        open:
          AWS_REGION: eu-west-1
----

This annotation is applied to the `suse-observability-ai-assistant` ServiceAccount created by the chart.

== Enable Anthropic models in AWS Bedrock

Before the first use of AI Assistant, make sure Anthropic Claude models are enabled in AWS Bedrock for the AWS account and region used by the AI Assistant backend.

If model access is not enabled, AI Assistant will not be able to invoke the required Bedrock models even if the IAM role and ServiceAccount are configured correctly.

Use the same AWS region that you configure for AI Assistant. If `AWS_REGION` is not set, the current AI Assistant backend default is `eu-west-1`.

. Open the AWS Bedrock console in the correct account and region.
. Open the `Model catalog`. If Anthropic models are not enabled yet, AWS shows a prompt to enable access.
+
image::ai-assistant/setup-aws/enable_anthropic_hint.png[Enable Anthropic model access in AWS Bedrock]

. Start the access request flow for Anthropic Claude models and enable them for your account.
+
image::ai-assistant/setup-aws/enable_anthropic_popup.png[Anthropic model access request in AWS Bedrock]

. After access is granted, verify that Anthropic Claude models are available in Bedrock.
+
image::ai-assistant/setup-aws/playground.png[Anthropic Claude models available in the AWS Bedrock playground]

. We recommend opening the Bedrock playground for `Claude Sonnet 4.6` and `Claude Opus 4.6` and sending a simple prompt such as `hello` to confirm that model access works correctly before using AI Assistant.

[NOTE]
====
The exact AWS console screens can differ slightly between regions and AWS accounts, but the required outcome is the same: Anthropic Claude models must be enabled in Bedrock before AI Assistant is used.

After model access is enabled in AWS Bedrock, it can take a few minutes before the models become usable in AI Assistant.
====

== Create the IAM policy and role

=== IAM policy permissions

Grant the AI Assistant role permission to invoke the Bedrock models used by the service. The current AI Assistant defaults use Anthropic Claude models through Bedrock, so the minimum required actions are:

* `bedrock:InvokeModel`
* `bedrock:InvokeModelWithResponseStream`

[NOTE]
====
Even though the current implementation uses Claude models, we recommend allowing these actions on all Bedrock models with `"Resource": ["*"]`. This helps avoid IAM policy updates when a future SUSE Observability release changes the default model set.
====

Recommended policy:

[,json]
----
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowAiAssistantBedrockInference",
      "Effect": "Allow",
      "Action": [
        "bedrock:InvokeModel",
        "bedrock:InvokeModelWithResponseStream"
      ],
      "Resource": ["*"]
    }
  ]
}

----

If you prefer a more restrictive policy, you can scope access to specific model ARNs, but you must review that policy on every upgrade.

=== IAM role trust policy

Create an IAM role that trusts the Kubernetes ServiceAccount used by the AI Assistant backend:

[,json]
----
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "Federated": "arn:aws:iam::123456789012:oidc-provider/oidc.eks.eu-west-1.amazonaws.com/id/EXAMPLE"
      },
      "Action": "sts:AssumeRoleWithWebIdentity",
      "Condition": {
        "StringEquals": {
          "oidc.eks.eu-west-1.amazonaws.com/id/EXAMPLE:aud": "sts.amazonaws.com",
          "oidc.eks.eu-west-1.amazonaws.com/id/EXAMPLE:sub": "system:serviceaccount:suse-observability:suse-observability-ai-assistant"
        }
      }
    }
  ]
}
----

Replace the AWS account ID, OIDC provider, AWS region, and Kubernetes namespace with your own values.

== Grant access to users with `execute-ai`

Users can only use AI Assistant when their role includes the `execute-ai` system permission.

Grant it with the `sts` CLI:

[,text]
----
sts rbac grant --subject ai-users --permission execute-ai
----

Verify it with:

[,text]
----
sts rbac describe-permissions --subject ai-users
----

In the current Helm chart permission definitions, `stackstate-admin`, `stackstate-power-user`, `stackstate-k8s-troubleshooter`, and `stackstate-k8s-admin` already include `execute-ai`.

If you manage access through Rancher RBAC, the permission maps to resource `ai` with verb `execute` in the `instance.observability.cattle.io` API group.

== Related information

For more details on related configuration patterns, see:

* xref:/setup/install-stackstate/kubernetes_openshift/customize_config.adoc[Override default configuration] for adding Helm values and extra environment variables
* xref:/setup/custom-integrations/overview.adoc[Custom Integrations Overview] for another example of enabling a feature through Helm values
* xref:/setup/security/rbac/rbac_permissions.adoc[Permissions] for the complete RBAC permission reference
* xref:/setup/security/rbac/rbac_roles.adoc[Roles] for predefined and custom role configuration
* xref:/setup/security/rbac/rbac_rancher.adoc[Rancher RBAC] for Rancher-managed roles and custom RoleTemplates
