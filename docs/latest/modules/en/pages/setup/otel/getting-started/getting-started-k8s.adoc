= Getting Started with Open Telemetry on Kubernetes
:page-languages: [en, de, es, fr, ja, pt, zh]
:revdate: 2025-07-10
:page-revdate: {revdate}
:description: {stackstate-product-name}

This guide provides instructions on monitoring an application.

* The monitored application/workload running in cluster A.
* The {stackstate-product-name} Agent telemetry gateway running in cluster A, receiving OTLP data from the observed application(s) and sending it to {stackstate-product-name}.
* {stackstate-product-name} running in cluster B, or SUSE Cloud Observability.

image::otel/open-telemetry-collector-kubernetes.png[Container instrumentation with the {stackstate-product-name} Agent telemetry gateway]

== Recommended path: {stackstate-product-name} Agent telemetry gateway

[NOTE]
====
The {stackstate-product-name} Agent telemetry gateway is the recommended path for Kubernetes workloads that are already instrumented with Open Telemetry SDKs.
====

Enable the OTel pipeline and the {stackstate-product-name} Agent telemetry gateway in your {stackstate-product-name} Agent values:

[,yaml]
----
otel:
  enabled: true
  telemetryGateway:
    enabled: true
----

Then point your application SDKs at the {stackstate-product-name} Agent telemetry gateway service. By default this is `suse-observability-agent-otel-telemetry-gateway` in namespace `suse-observability-agent`.

[TIP]
====
If your workloads expose Prometheus / OpenMetrics endpoints and are not instrumented with an OpenTelemetry SDK, use the scrape-based path instead: xref:/use/metrics/k8s-otel-prometheus-scraping.adoc[Scraping OpenMetrics with ServiceMonitor and PodMonitor].
====

Set the following environment variables on your application pods:

[,bash]
----
OTEL_EXPORTER_OTLP_ENDPOINT=http://suse-observability-agent-otel-telemetry-gateway.suse-observability-agent.svc.cluster.local:4317
OTEL_TRACES_EXPORTER=otlp
OTEL_METRICS_EXPORTER=otlp
OTEL_LOGS_EXPORTER=none
OTEL_SERVICE_NAME=<your-service-name>
----

For HTTP/protobuf configuration, `POD_UID` downward API injection, attribute ownership, network policy requirements, and scaling guidance, see xref:/setup/otel/telemetry-gateway.adoc#_configure_your_sdks[Configure your SDKs], xref:/setup/otel/telemetry-gateway.adoc#_attribute_ownership_security_enforcement[Attribute ownership], xref:/setup/otel/telemetry-gateway.adoc#_network_policies[Network policies], and xref:/setup/otel/telemetry-gateway.adoc#_scaling[Scaling].

== Collect telemetry data from your application

The common way to collect telemetry data is to instrument your application using the Open Telemetry SDK's.

* xref:/setup/otel/instrumentation/java.adoc[Java]
* xref:/setup/otel/instrumentation/dot-net.adoc[.NET]
* xref:/setup/otel/instrumentation/node.js.adoc[Node.js]

For other languages follow the documentation on https://opentelemetry.io/docs/languages/[opentelemetry.io] and configure the SDK exporter to ship data to the {stackstate-product-name} Agent telemetry gateway by following xref:/setup/otel/instrumentation/sdk-exporter-config.adoc[these instructions].

== View the results

Go to {stackstate-product-name} and make sure the Open Telemetry Stackpack is installed (via the main menu \-> Stackpacks).

If your pods are getting traffic, you should be able to find them under their service name in the Open Telemetry \-> services and service instances overviews. Traces appear in the xref:/use/traces/k8sTs-explore-traces.adoc[trace explorer] and in the xref:/use/views/k8s-traces-perspective.adoc[trace perspective] for the service and service instance components. Span metrics and language specific metrics (if available) become available in the xref:/use/views/k8s-metrics-perspective.adoc[metrics perspective] for the components.

If you also have the Kubernetes stackpack installed the instrumented pods will also have the traces available in the xref:/use/views/k8s-traces-perspective.adoc[trace perspective].

== Rancher RBAC

For xref:/setup/security/rbac/rbac_rancher.adoc[Rancher RBAC] to work, telemetry data needs to have the following resource attributes present:

* `k8s.cluster.name` - the *Cluster* name as used by the Kubernetes stackpack
* `k8s.namespace.name` - a *Namespace* managed by a Rancher *Project*

With the {stackstate-product-name} Agent telemetry gateway path in this guide, these attributes are enriched by the gateway and sourced from Kubernetes metadata.

== Next steps

You can add new charts to components by creating a xref:/setup/custom-integrations/presentation/concepts.adoc[Component Presentation]. For a service or service instance in your application, follow xref:/setup/custom-integrations/presentation/metrics/README.adoc[our guide]. It is also possible to create xref:/use/alerting/k8s-monitors.adoc[new monitors] using the metrics and setup xref:/use/alerting/notifications/configure.adoc[notifications] to get notified when your application is not available or having performance issues.

== More info

* xref:/use/security/k8s-service-tokens.adoc[Service tokens]
* xref:/setup/otel/otlp-apis.adoc[Open Telemetry API]
* xref:/setup/otel/telemetry-gateway.adoc[Telemetry gateway]
* xref:/setup/otel/instrumentation/README.adoc[Open Telemetry SDKs]
