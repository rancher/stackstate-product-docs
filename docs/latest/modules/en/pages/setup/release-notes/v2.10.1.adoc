= v2.10.1 - 28/May/2026
:page-languages: [en, de, es, fr, ja, pt, zh]
:revdate: 2026-05-28
:page-revdate: {revdate}
:description: SUSE Observability Self-hosted

== Release Notes: {stackstate-product-name} Helm Chart v2.10.1

== Component Versions

[cols="1,2"]
|===
| Component | Version

| *SUSE Observability* | `2.10.1`
| *SUSE Observability Agent* | `1.3.40`
|===


TIP: When upgrading from an older version of the platform, be sure to check out our xref:setup/upgrade-stackstate/version-specific-upgrade-instructions.adoc[version-specific upgrade instructions].

== Security Updates

Security: No Known Affected CVEs at Release Time
At the time of this tested SUSE Observability release, we have no known CVEs that affect SUSE Observability across Low, Medium, High, or Critical severities.
This is based on our vulnerability scans together with the reviewed context in the link:https://github.com/StackVista/vexhub[SUSE Observability VEX Hub]. The VEX Hub explains which reported CVEs do not affect SUSE Observability as shipped.
New CVEs may appear after a release. To reduce the time between fixes, we now also publish daily pre-releases. These are available through Helm with the --devel flag and can include CVE fixes before the next tested release.
These release notes apply to our tested releases, which are published roughly three times every two months. Our aim is simple: each tested release should ship with no known CVEs that affect SUSE Observability..

== New Features & Enhancements

* *Notification Toast Messages:* Disabled notification toast messages within the user interface to reduce alert noise.

== Bug Fixes

* *Topology and Overview Filter Resolution:* ** Fixed an issue where topology filters failed to display on the Overview and Topology perspectives for views configured to show them.
** Resolved a bug where configured topology filters were not being properly applied to the data.
** Fixed an issue where overview filters were incorrectly applied to views where they did not belong.
* *OpenMetrics Endpoint 500 Error:* Fixed a bug where the OpenMetrics endpoint of the main `stackstate-server` Docker images returned a `500 Internal Server Error` due to the accidental introduction of duplicate labels.
* *View Loading and Management:* ** Fixed an issue where views missing a specified `viewtype` would fail to load with a `Failed to extract identifier type` error.
** Resolved a UI bug where a deleted view would remain visible in the views list until the browser page was manually refreshed.
* *Platform Log Spam:* Fixed a platform issue where an HBase region server would continuously spam the log files with `Not a regular file` errors.

== Agent Bug Fixes

* No agent-specific bug fixes were reported in this release.