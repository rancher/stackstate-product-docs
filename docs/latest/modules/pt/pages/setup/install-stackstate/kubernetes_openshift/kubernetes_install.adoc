= Instale o {stackstate-product-name}
:revdate: 2025-07-14
:page-revdate: {revdate}
:description: {stackstate-product-name} Auto-hospedado

== Antes de começar

[NOTE]
====
Notas adicionais para instalação em:

* *Clusters Kubernetes com permissões limitadas*: Leia as xref:/setup/install-stackstate/kubernetes_openshift/required_permissions.adoc[permissões necessárias].
* *Alibaba Cloud Container Service for Kubernetes (ACK)*: Leia as xref:/setup/install-stackstate/kubernetes_openshift/ack.adoc[notas de instalação do ACK].
* *OpenShift*: Consulte as xref:/setup/install-stackstate/kubernetes_openshift/openshift_install.adoc[instruções de instalação do OpenShift].
====


Antes de iniciar a instalação do {stackstate-product-name}:

* Verifique os xref:/setup/install-stackstate/requirements.adoc[requisitos] para garantir que seu ambiente Kubernetes se encaixa na configuração que você usará (recomendada, mínima ou não alta disponibilidade).
* Verifique se você possui as xref:/setup/install-stackstate/kubernetes_openshift/required_permissions.adoc[permissões necessárias].
* Adicione o repositório helm {stackstate-product-name} ao cliente helm local:

[,text]
----
helm repo add suse-observability https://charts.rancher.com/server-charts/prime/suse-observability
helm repo update
----

== Instale o {stackstate-product-name}

[NOTE]
====
Para ambientes sem acesso à internet, também conhecidos como ambientes air-gapped, siga primeiro xref:/k8s-suse-rancher-prime-air-gapped.adoc[estas instruções adicionais].

Além disso, certifique-se de seguir as instruções de instalação para ambientes air-gapped sempre que elas estiverem presentes para uma etapa.
====


. xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc#_create_namespace[Criar o namespace onde {stackstate-product-name} será instalado]
. xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc#create-values-yaml[Criar o arquivo de configuração do `values.yaml`]
. xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc#_configure_storage_class_optional[Configurar a classe de armazenamento (Opcional)]
. xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc#_deploy_suse_observability_with_helm[Implantar {stackstate-product-name} com Helm]
. xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc#_access_the_suse_observability_ui[Acesse a interface do usuário {stackstate-product-name}]


=== Criar namespace

Comece criando o namespace onde você deseja instalar {stackstate-product-name} e implantar o segredo nesse namespace. Em nosso guia, usaremos o namespace `suse-observability`:

[,text]
----
kubectl create namespace suse-observability
----

[#create-values-yaml]
=== Criar arquivo de configuração `values.yaml`

O arquivo `values.yaml` contém toda a configuração necessária para implantar {stackstate-product-name}, incluindo sua chave de licença, URL base, senha de administrador e perfil de dimensionamento.

[NOTE]
====
*Antes de continuar:* Certifique-se de ter a versão mais recente dos gráficos Helm com `helm repo update`.
====

[tabs]
====
Método recomendado::
+
--
[NOTE]
=====
O método de configuração `global.suseObservability` está disponível a partir da versão `2.8.0`. Para versões anteriores, use o método legado.
=====

Crie um arquivo `values.yaml` com a estrutura de configuração `global.suseObservability`:

[source,yaml]
----
global:
  # Optional: Override image registry (defaults to registry.rancher.com)
  # Only needed for air-gapped environments or custom registries
  # imageRegistry: "your-private-registry.example.com"

  # Optional: Override storage class (uses cluster default if not set)
  # storageClass: "your-storage-class"

  suseObservability:
    # Required: Your {stackstate-product-name} license key
    license: "YOUR-LICENSE-KEY"

    # Required: Base URL for {stackstate-product-name}
    # Use http://localhost:8080 if ingress is not configured yet
    baseUrl: "https://observability.example.com"

    # Required: Sizing profile
    # Available: trial, 10-nonha, 20-nonha, 50-nonha, 100-nonha,
    #            150-ha, 250-ha, 500-ha, 4000-ha
    sizing:
      profile: "150-ha"

    # Required: Plain text admin password
    adminPassword: "your-password"
    # Instead of adminPassword you can provide a bcrypt hashed password with adminPasswordBcrypt
    # Generate with: htpasswd -bnBC 10 "" "your-password" | tr -d ':\n'
    # adminPasswordBcrypt: "$2a$10$..."

    # Optional: Receiver API key (auto-generated if not provided)
    # receiverApiKey: "your-receiver-api-key"

    # Optional: Pull secret for private registry
    # pullSecret:
    #   username: "registry-user"
    #   password: "registry-pass"
----

Para mais opções de configuração, incluindo configurações de afinidade, veja xref:/setup/install-stackstate/kubernetes_openshift/affinity.adoc[Configurar Afinidades do Kubernetes].

--
Método legado (Descontinuado)::
+
--
[WARNING]
=====
Este método está descontinuado. Para novas instalações, use o método recomendado acima. Para instalações existentes que usam este método, consulte o xref:/setup/upgrade-stackstate/migrate-to-global-mode.adoc[guia de migração] para transitar para o novo formato de configuração.
=====

Você pode gerar os arquivos de configuração {stackstate-product-name} executando um gráfico Helm separado. Este é o gráfico `suse-observability/suse-observability-values`:

[,text]
----
export VALUES_DIR=.
helm template \
  --set license='<your license>' \
  --set baseUrl='<suse-observability-base-url>' \
  --set sizing.profile='<sizing.profile>' \
  suse-observability-values \
  suse-observability/suse-observability-values --output-dir $VALUES_DIR
----

Este comando gera os arquivos `$VALUES_DIR/suse-observability-values/templates/baseConfig_values.yaml`, `$VALUES_DIR/suse-observability-values/templates/sizing_values.yaml` e `$VALUES_DIR/suse-observability-values/templates/affinity_values.yaml`.

[NOTE]
=====
As senhas de administrador do {stackstate-product-name} serão geradas automaticamente pelo comando acima e são exibidas como comentários no arquivo `basicConfig.yaml` gerado. Os valores reais contêm os hashes `bcrypt` dessas senhas para que sejam armazenados de forma segura na liberação do Helm no cluster.
=====

Os valores que podem ser passados para este gráfico são:

[cols="1,1,2"]
|===
| Configuração | Valor | Descrição

| Gerar Configuração Básica
| `basicConfig.generate`
| Ative ou desative a geração do arquivo `basicConfig_values.yaml`.

| Chave da API do Receptor
| `receiverApiKey`
| A chave da API usada pelo {stackstate-product-name} para receber dados dos agentes. Esta é uma chave secreta que deve ser mantida em privado. Se você omitir isso, uma chave aleatória será gerada para você.

| URL de Base
| `baseUrl`
| O `<STACKSTATE_BASE_URL>`. A URL externa para {stackstate-product-name} que usuários e agentes usarão para se conectar. Por exemplo `https://suse-observability.internal`. Se você ainda não decidiu sobre uma configuração de Ingress, use `http://localhost:8080`. Isso pode ser atualizado mais tarde no arquivo gerado.

| Nome de usuário e senha**
| `-u` `-p`
| O nome de usuário e a senha usados pelo {stackstate-product-name} para puxar imagens. Para ambientes air-gapped, esses precisam ser o nome de usuário e a senha do registro de contêiner local.

| Chave de licença
| `license`
| A chave de licença do {stackstate-product-name}.

| Senha padrão
| `adminPassword`
| A senha para o usuário padrão (`admin`) acessar a interface do {stackstate-product-name}. Alternativamente, você pode fornecer uma senha hash bcrypt usando `adminPasswordBcrypt`. Apenas uma das duas opções pode ser usada ao mesmo tempo.

| Registro de Imagens
| `imageRegistry`
| O registro onde as imagens {stackstate-product-name} estão hospedadas. Se não fornecido, o valor padrão será 'registry.rancher.com'. Apenas necessário para ambientes air-gapped ou registros personalizados.

| Nome de Usuário do Pull Secret
| `pullSecret.username`
| O nome de usuário usado para puxar imagens do registro Docker onde as imagens {stackstate-product-name} estão hospedadas. Apenas necessário para registros personalizados.

| Senha do Pull Secret
| `pullSecret.password`
| A senha usada para puxar imagens do registro Docker onde as imagens {stackstate-product-name} estão hospedadas. Apenas necessário para registros personalizados.

| Geração de Tamanhos
| `sizing.generate`
| Ative ou desative a geração do arquivo `sizing_values.yaml`.

| Perfil de Tamanhos
| `sizing.profile`
| Um dos: trial, 10-nonha, 20-nonha, 50-nonha, 100-nonha, 150-ha, 250-ha, 500-ha. Com base nesses perfis, você pode gerar o arquivo `sizing_values.yaml` contendo tamanhos padrão para os recursos {stackstate-product-name} e configuração para implantar em modo HA ou Non-HA. Por exemplo, 10-nonha produz um `sizing_values.yaml` para implantar uma instância Non-HA {stackstate-product-name} para observar um cluster de 10 nós em modo não alta disponibilidade. Atualmente, mover de um ambiente Non-HA para um ambiente HA não é possível. Se você espera que seu ambiente precise observar cerca de 150 nós, então é melhor optar por HA imediatamente.

| Anti-Afinidade de Pod
| `affinity.podAntiAffinity.requiredDuringSchedulingIgnoredDuringExecution`
| Imponha que as réplicas dos mesmos serviços de dados sejam agendadas em diferentes nós do Kubernetes. Apenas aplicável para `HA` perfis. Ou `true` ou `false`. O padrão é `true` - o agendamento em diferentes nós é imposto.
|===

[NOTE]
=====
Armazene os arquivos `basicConfig.yaml` e `sizing_values.yaml` gerados com segurança. Você pode reutilizar esses arquivos para atualizações, economizando tempo e garantindo que {stackstate-product-name} continue a usar a mesma chave de API. Isso é desejável, pois significa que os Agentes e outros provedores de dados para {stackstate-product-name} não precisarão ser atualizados.
Você pode regenerar os arquivos de forma independente usando os switches `basicConfig.generate=false` e `sizing.generate=false` para desativar qualquer um deles, mantendo a versão previamente gerada do arquivo no `output-dir`.
=====

[NOTE]
=====
O gráfico de Valores {stackstate-product-name} gera configurações de afinidade que você pode usar com o gráfico principal {stackstate-product-name} para controlar o comportamento de agendamento de pods. Consulte a documentação xref:/setup/install-stackstate/kubernetes_openshift/affinity.adoc[Configurar Afinidades do Kubernetes] para mais informações.
=====

--
====

=== Configurar classe de armazenamento (Opcional)

{stackstate-product-name} usa a classe de armazenamento padrão do cluster se não for especificado. Você pode opcionalmente substituir isso definindo `global.storageClass` no seu arquivo `values.yaml`.

[tabs]
====
Método recomendado::
+
--
Se você precisar usar uma classe de armazenamento específica, adicione-a ao seu arquivo `values.yaml` sob `global.storageClass`:

[source,yaml]
----
global:
  storageClass: "your-storage-class"  # e.g., "local-path" for Rancher local-path provisioner
  suseObservability:
    # ... other configuration
----
--
Método legado (Descontinuado)::
+
--
Edite o arquivo `$VALUES_DIR/suse-observability-values/templates/baseConfig_values.yaml` para adicionar a configuração "storageClass":

[,text]
----
# Source suse-observability-values/templates/baseConfig_values.yaml
global:
  storageClass: "your-storage-class" # Optional: override default storage class
----
--
====


=== Implante {stackstate-product-name} com Helm

A implantação recomendada de {stackstate-product-name} é uma configuração de alta disponibilidade pronta para produção, com muitos serviços executando de forma redundante. Se necessário, também é possível executar {stackstate-product-name} em uma configuração não redundante, onde cada serviço tem apenas uma única réplica. Essa configuração é recomendada apenas para um ambiente de teste.

Para ambientes air-gapped, siga as instruções para as instalações air-gapped.

[tabs]
====
Método recomendado::
+
--

Para implantar {stackstate-product-name} no Kubernetes:

. Antes de implantar:
 ** xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc#_create_namespace[Criar o namespace onde {stackstate-product-name} será instalado]
 ** <<create-values-yaml,Criar o arquivo de configuração do `values.yaml`>>
. Implante a versão mais recente do {stackstate-product-name} no namespace `suse-observability` com o seguinte comando:

[,text]
----
helm upgrade \
  --install \
  --namespace suse-observability \
  --values values.yaml \
suse-observability \
suse-observability/suse-observability
----

Alternativamente, implante diretamente usando as flags do `--set` sem um arquivo de valores:

[,bash]
----
helm upgrade \
  --install \
  --namespace suse-observability \
  --set global.suseObservability.license="YOUR-LICENSE-KEY" \
  --set global.suseObservability.baseUrl="https://observability.example.com" \
  --set global.suseObservability.sizing.profile="150-ha" \
  --set global.suseObservability.adminPassword='$2a$10$...' \
suse-observability \
suse-observability/suse-observability
----

--
Método recomendado (air-gapped)::
+
--

Para implantar {stackstate-product-name} em um setup air-gapped no Kubernetes:

. Antes de implantar:
 ** xref:/k8s-suse-rancher-prime-air-gapped.adoc[Siga estas instruções adicionais para instalações air-gapped].
 ** xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc#_create_namespace[Criar o namespace onde {stackstate-product-name} será instalado]
 ** <<create-values-yaml,Criar o arquivo de configuração do `values.yaml`>>
. Implante a versão mais recente do {stackstate-product-name} no namespace `suse-observability` com o seguinte comando:

[,bash]
----
helm upgrade \
  --install \
  --namespace suse-observability \
  --values local-docker-registry.yaml \
  --values values.yaml \
suse-observability \
suse-observability/suse-observability
----

--
Método legado (Descontinuado)::
+
--

[WARNING]
=====
Este método está descontinuado. Para novas instalações, use o método recomendado acima. Para instalações existentes que usam este método, consulte o xref:/setup/upgrade-stackstate/migrate-to-global-mode.adoc[guia de migração] para transitar para o novo formato de configuração.
=====

Para implantar {stackstate-product-name} no Kubernetes:

. Antes de implantar:
 ** xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc#_create_namespace[Criar o namespace onde {stackstate-product-name} será instalado]
 ** <<create-values-yaml,Gere `baseConfig_values.yaml` e `sizing_values.yaml`>>
. Implante a versão mais recente do {stackstate-product-name} no namespace `suse-observability` com o seguinte comando:

[,bash]
----
helm upgrade \
  --install \
  --namespace suse-observability \
  --values $VALUES_DIR/suse-observability-values/templates/baseConfig_values.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/sizing_values.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/affinity_values.yaml \
suse-observability \
suse-observability/suse-observability
----

--
Método legado (air-gapped)::
+
--

[WARNING]
=====
Este método está depreciado. Para novas instalações, use o método recomendado acima. Para instalações existentes que usam este método, consulte o xref:/setup/upgrade-stackstate/migrate-to-global-mode.adoc[guia de migração] para transitar para o novo formato de configuração.
=====

Para implantar {stackstate-product-name} em um setup air-gapped no Kubernetes:

. Antes de implantar:
 ** xref:/k8s-suse-rancher-prime-air-gapped.adoc[Siga estas instruções adicionais para instalações air-gapped].
 ** xref:/setup/install-stackstate/kubernetes_openshift/kubernetes_install.adoc#_create_namespace[Crie o namespace onde {stackstate-product-name} será instalado]
 ** <<create-values-yaml,Gere `baseConfig_values.yaml` e `sizing_values.yaml`>>
. Implante a versão mais recente do {stackstate-product-name} no namespace `suse-observability` com o seguinte comando:

[,bash]
----
helm upgrade \
  --install \
  --namespace suse-observability \
  --values local-docker-registry.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/baseConfig_values.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/sizing_values.yaml \
  --values $VALUES_DIR/suse-observability-values/templates/affinity_values.yaml \
suse-observability \
suse-observability-A.B.C.tgz
----

--
====

Após a instalação, a versão do {stackstate-product-name} deve estar listada no namespace {stackstate-product-name} e todos os pods devem estar em execução:

[,text]
----
# Check the release is listed
helm list --namespace suse-observability

# Check pods are running
# It may take some time for all pods to be installed or available
kubectl get pods --namespace suse-observability
----

=== Acesse a interface do usuário do {stackstate-product-name}

Após a implantação do {stackstate-product-name}, você pode verificar se todos os pods estão ativos e em execução:

[,bash]
----
kubectl get pods --namespace suse-observability
----

Quando todos os pods estiverem ativos, você pode habilitar um redirecionamento de porta:

[,bash]
----
kubectl port-forward service/<helm-release-name>-suse-observability-router 8080:8080 --namespace suse-observability
----

Para a configuração de ingress, crie um arquivo `ingress_values.yaml` e adicione o código abaixo.

[NOTE]
====
Lembre-se de definir um segredo para a configuração TLS. Para detalhes sobre como fazer isso, consulte a documentação do kubectl.
====

[,yaml]
----
ingress:
  enabled: true
  class: ""
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    #if external dns (cloudflare and similars) configure:
    #external-dns.alpha.kubernetes.io/target: <INGRESS_FQDN>
    #external-dns.alpha.kubernetes.io/ttl: "60"
    #end if
  hosts:
  - host: <SUSE_OBSERVABILITY_HOST>
  tls:
  - hosts:
    - <SUSE_OBSERVABILITY_HOST>
    secretName: suse-private-ai-tls
  existingSecret: suse-private-ai-tls
  # Set up your secrets in whichever way feels fit. Check k8s docs for details: https://kubernetes.io/docs/reference/kubectl/generated/kubectl_create/kubectl_create_secret_tls/
----

Prossiga para fazer upgrade para aplicar a configuração de ingress:

[tabs]
====
Upgrade::
+
--

[,bash]
----
helm upgrade --namespace suse-observability --reuse-values --values ingress_values.yaml suse-observability suse-observability/suse-observability
----

--
Fazer upgrade air-gapped::
+
--

[,bash]
----
helm upgrade --namespace suse-observability --reuse-values --values suse-observability-values/templates/ingress_values.yaml suse-observability suse-observability-A.B.C.tgz
----

--
====

[NOTE]
====
Se você não estiver usando um DNS externo, adicione ao seu computador local /etc/hosts: <CLUSTER_IP> <SUSE_OBSERVABILITY_HOST>.
====

{stackstate-product-name} agora estará disponível no seu navegador em `https://localhost:8080`. Faça login com o nome de usuário `admin` e a senha padrão fornecida no arquivo `baseConfig_values.yaml`.

As próximas etapas são

* xref:/setup/install-stackstate/kubernetes_openshift/ingress.adoc[Exponha {stackstate-product-name} fora do cluster]
* xref:/k8s-quick-start-guide.adoc[Comece a monitorar seus clusters Kubernetes]
* Dê acesso aos seus xref:/setup/security/authentication/README.adoc[colegas de trabalho].
