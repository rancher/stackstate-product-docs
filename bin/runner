#!/bin/bash

docs_dir=$1

function runner {
  echo Processing $1
  echo Admonitions
  python ./fix-admon.py $1
  echo Image paths
  ./img_path.sh $1
  echo Tabbed content
  ./tabs.sh $1
}

export -f runner

find $docs_dir -type f -name '*.adoc' -exec bash -c 'runner $0' {} \;